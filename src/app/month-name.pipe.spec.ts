import { AkMonthNamePipe } from './month-name.pipe';

describe('MonthNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AkMonthNamePipe();
    expect(pipe).toBeTruthy();
  });
});
